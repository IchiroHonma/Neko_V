2019-05-12-VBA-filesystemアクセス

[VBAでファイルシステムにアクセスするのは意外と難しい]
FSOの/obj/

/obj/ = createobject("scripting.filesystemobject")

[FileSystemObject]
参照設定なし（おすすめ）だと
FileSystemObject型が無い

よって
Dim a as Object
で扱う必要がある
Variantでもいいけど…

[各レイヤーへのアクセス]
'FileSystemObjectへのアクセス ===   CreateObject("Scripting.FileSystemObject")  以後 myFSOと訳す
'Drivesへのアクセス                 myFSO.Drives
'Driveへのアクセス                  myFSO.Drives("具体名")
'                                   myFSO.getDrive ("具体名")
'                                   myFSO.getFolder("具体名").Drive
'そもそも driveとfolderは似て非なるものであり、今自分が想定しているのはだいたいfolderの方だ
'Foldersへのアクセス                どうするの？
'                                   myFSO.Drives.RootFolder.SubFolders
'Folders                            myFSO.getFolder("具体名").SubFolder
'Folders                            myFSO.getFolder("具体名").ParentFolder.SubFolder
'
'Folder
'カレントフォルダ                   myFSO.getFolder("./")

'Files                              myFSO.getFile("PATH")
'カレントファイルは取れるだろうか   myFSO.getFile(activeworkbook.path & "\" & activeworkbook.name)
'                                   myFSO.getFile("./" & activeworkbook.name)


自分から


[WSF:index関数]
http://office-qa.com/Excel/ex48.htm
http://blog.jmiri.net/?p=1886

.index(範囲,R,C)	は 範囲からvalueを返す
.match(検索値,行もしくは列,条件)は　値から行範囲もしくは列範囲のindex番号を返す

セル範囲形式： INDEX(参照, 行番号, [列番号], [領域番号])
配列形式： INDEX(配列, 行番号, [列番号])

MATCH(検査値, 検査範囲, [照合の種類])

照合の種類は1、0、-1で指定し（省略すると1）、以下の意味を持ちます。
1：検査値以下の最大値を検索（検査範囲は昇順ソートが必要）
0：完全一致
-1：検査値以上の最小値を検索（検査範囲は降順ソートが必要）
0じゃないと使いづらいよお

